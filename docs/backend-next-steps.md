# 后端后续工作清单

本文档只记录后端需要继续完善的部分。

## 当前后端基线

当前后端已具备完整 MVP + P1 能力：

| 模块 | 当前状态 |
| --- | --- |
| 认证 | 邮箱密码注册、登录、退出、当前用户，HttpOnly Cookie，限流，密码策略，设备信息，会话管理 |
| 数据库 | PostgreSQL + Prisma，核心业务表已迁移，包括 WishStatusHistory |
| 情侣空间 | 创建、邀请码加入（7天过期、可刷新）、详情、更新、删除（Owner-only） |
| 关系数据 | 纪念日、日记、心情、愿望（含状态历史）、照片记录、时间胶囊 CRUD |
| Agent | DeepSeek provider，关系助手，工具调用（草稿确认），记忆管理 CRUD |
| 前端 | 完整前端：登录/注册、Dashboard、日记/心情/纪念日/愿望 CRUD、设置页 |
| 测试 | API E2E 脚本（含纪念日、心情、愿望、照片、授权、校验测试）和 DeepSeek smoke 脚本 |

## P0：后端必须补强

| 工作项 | 目标 | 状态 |
| --- | --- | --- |
| 认证安全增强 | 登录/注册限流，密码策略 | ✅ 已完成 |
| 会话管理 | 设备信息、会话列表、撤销 | ✅ 已完成 |
| 权限策略统一 | 照片/愿望 Owner 校验，Couple 标题 Owner-only | ✅ 已完成 |
| API 错误规范 | Agent 路由统一使用 ApiError | ✅ 已完成 |
| 输入校验补齐 | 愿望状态参数校验（移除 `as never`） | ✅ 已完成 |
| 接口 E2E 扩展 | 覆盖纪念日、心情、愿望、照片、授权、校验 | ✅ 已完成 |
| 密码找回 | 重置 token、过期、邮件接口 | ❌ 待实现 |
| 邮箱验证 | 验证 token、重发、未验证限制 | ❌ 待实现 |

## P1：核心业务完善

| 工作项 | 目标 | 状态 |
| --- | --- | --- |
| 邀请机制升级 | 7天过期、可刷新 | ✅ 已完成 |
| 心情趋势接口 | 按天/周/月聚合 | ✅ 已完成 |
| 愿望清单协作 | 状态流转历史记录 | ✅ 已完成 |
| 时间胶囊安全 | 服务层锁定，已开启不可编辑 | ✅ 已完成 |
| Agent 记忆管理 | CRUD AgentMemory | ✅ 已完成 |
| Agent 工具调用 | 草稿生成 + 确认写入 | ✅ 已完成 |
| 情侣解绑流程 | 解绑申请、确认、冷静期 | ❌ 待实现 |
| 日记权限细化 | PRIVATE/PARTNER/SHARED 测试 | ❌ 待实现 |

## P2：文件、通知与异步任务

| 工作项 | 目标 | 验收标准 |
| --- | --- | --- |
| 图片上传 | 从 URL 元数据升级为真实相册能力 | 接入 S3/R2/Supabase Storage；限制大小、类型、数量 |
| 图片处理 | 降低存储和加载成本 | 生成缩略图、记录宽高、支持封面图 |
| 通知系统 | 支持纪念日、心情、任务提醒 | 建立 Notification 表；支持站内通知和邮件/短信适配 |
| 定时任务 | 处理到期提醒、时间胶囊、周报 | 引入 cron/队列；任务幂等；失败可重试 |
| 关系报告生成 | 支持周报/月报/年度回忆 | 后端聚合数据后调用 Agent 生成草稿，用户确认保存 |

## P3：生产化能力

| 工作项 | 目标 | 验收标准 |
| --- | --- | --- |
| 日志与监控 | 线上问题可定位 | 请求日志、错误日志、Agent 调用日志、慢查询记录 |
| 限流与风控 | 防止接口被刷 | 登录、注册、Agent、上传接口分别限流 |
| 审计日志 | 敏感操作可追踪 | 删除、解绑、权限变更、Agent 写入确认都有审计记录 |
| 数据导出 | 尊重用户数据所有权 | 用户可导出自己的数据；情侣空间可导出共同数据 |
| 数据删除策略 | 删除行为可解释、可恢复 | 软删除或回收期策略；明确级联删除范围 |
| 备份与恢复 | 避免数据丢失 | 数据库定期备份；恢复流程有演练文档 |
| 部署配置 | 方便上线和迁移 | 生产 `.env` 模板、`db:deploy` 流程、健康检查、启动脚本 |

## API 测试计划

| 类型 | 覆盖内容 |
| --- | --- |
| 单元测试 | 密码哈希、会话 Cookie、权限 guard、Agent prompt 构造 |
| API E2E | 注册登录、情侣空间、资源 CRUD、越权、退出登录 |
| 数据库测试 | 迁移状态、外键约束、级联删除、唯一索引 |
| Agent 测试 | fallback、DeepSeek smoke、工具草稿输出、失败降级 |
| 安全测试 | 错误密码、过期会话、跨用户访问、跨空间访问 |

## 推荐开发顺序

1. 先补齐认证安全：邮箱验证、密码找回、登录限流、会话管理。
2. 再补业务权限测试：所有资源 CRUD 都覆盖成员、作者、创建者边界。
3. 然后做 Agent 工具调用：先生成草稿，用户确认后写库。
4. 接着做图片上传和对象存储。
5. 最后做通知、定时任务、审计日志和生产部署。

## 近期最建议完成的 5 件事

| 优先级 | 任务 |
| --- | --- |
| 1 | 扩展 `test:api`，覆盖全部业务资源 CRUD 和越权场景 |
| 2 | 加登录/注册/Agent 接口限流 |
| 3 | 实现邮箱验证和密码找回 |
| 4 | 增加 Agent 写入草稿与用户确认机制 |
| 5 | 接入对象存储，完成真实相册上传后端 |
